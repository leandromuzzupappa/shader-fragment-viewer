{
  "name": "shader-fragment-viewer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^3.1.0"
  },
  "dependencies": {
    "monaco-editor": "^0.34.1",
    "three": "^0.145.0"
  }
}
